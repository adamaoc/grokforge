/** Re-export shared handoff builder (story 113). */
export { buildVoiceHandoffUserText } from '../../../shared/voice-agent-handoff'
export type {
  BuildVoiceHandoffUserTextInput,
  VoiceHandoffLine,
} from '../../../shared/voice-agent-handoff'
