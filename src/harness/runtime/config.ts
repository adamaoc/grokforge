/** Tool loop cap (ampnet default). */
export const HARNESS_MAX_TOOL_ITERATIONS = 25

/** Non-streaming xAI request timeout for a single model step. */
export const HARNESS_CHAT_SAMPLE_TIMEOUT_MS = 90_000
